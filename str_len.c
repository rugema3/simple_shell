#include"shell.h"
/**
 * get_str_length - Returns the length of a string.
 * @input_string: Type char pointer
 * Return: Always 0.
 */
int get_str_length(const char *input_string)
{
	int length;

	for (length = 0; input_string[length] != 0; length++)
	{
	}
	return (length);
}
